# Property Management System (PMS)

A TypeScript-based Property Management System with separate client and server components.

## Setup

### Prerequisites
- Node.js (v16 or higher)
- npm or yarn

### Client Setup
```bash
cd client
npm install
npm run dev
```

### Server Setup
cd server
npm install
# Copy .env.example to .env and configure
npm run dev